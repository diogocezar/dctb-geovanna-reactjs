import React, { Component } from 'react'

import './carousel.css'

class Carousel extends Component {
    render() {
        return (
            <h1>Implements here!</h1>
        );
    }
}

export default Carousel